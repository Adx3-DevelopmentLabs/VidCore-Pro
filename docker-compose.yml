services:
  vidcore-pro:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - CACHE_TTL=86400
      - MAX_WORKERS=15
    restart: unless-stopped
